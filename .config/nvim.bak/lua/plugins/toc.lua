return {
  {
    "richardbizik/nvim-toc",
    -- Add any plugin-specific configuration here
    config = function()
      -- Example: set a default option, if nvim-toc supports configuration
      -- require('nvim-toc').setup {
      --   option = value,
      -- }
    end,
  },
}
